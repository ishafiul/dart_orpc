# dart_orpc_luthor

The Luthor schema and validation bridge for `dart_orpc` DTO contracts. It keeps
runtime validation and generated transport metadata aligned with the same DTO
definitions.

Most applications should use the
[`dart_orpc`](https://pub.dev/packages/dart_orpc) facade package.
