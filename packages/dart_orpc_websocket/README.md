# dart_orpc_websocket

WebSocket server and VM client transport for `dart_orpc`, implementing the
`dart-orpc.v1` protocol for unary procedures and typed server streams.

Most applications should use the
[`dart_orpc`](https://pub.dev/packages/dart_orpc) facade package.
