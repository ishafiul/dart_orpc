name: dart_orpc_cli
description: CLI tooling for dart_orpc development workflows.
version: 0.1.0-dev.1
publish_to: none
resolution: workspace

environment:
  sdk: ^3.11.0

executables:
  dart_orpc: dart_orpc

dependencies:
  args: ^2.7.0
  yaml: ^3.1.3

dev_dependencies:
  test: ^1.26.3
