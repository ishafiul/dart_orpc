# dart_orpc_annotations

Public annotations used to define `dart_orpc` modules, controllers, procedures,
DTO input bindings, and REST/SSE mappings for compile-time generation.

Most applications should depend on the
[`dart_orpc`](https://pub.dev/packages/dart_orpc) facade package.
