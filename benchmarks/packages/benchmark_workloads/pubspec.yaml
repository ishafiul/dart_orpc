name: benchmark_workloads
description: Shared deterministic workloads for framework benchmark fixtures.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dev_dependencies:
  test: ^1.26.0
