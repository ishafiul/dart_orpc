name: shelf_benchmark
description: Runtime benchmark fixture for Shelf.
version: 0.1.0
publish_to: none

environment:
  sdk: ^3.11.0

dependencies:
  benchmark_workloads:
    path: ../../packages/benchmark_workloads
  shelf: ^1.4.2
  shelf_router: ^1.1.4
