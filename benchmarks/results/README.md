# Benchmark results

Raw local result files are intentionally ignored. Commit a dated result set
only when it includes machine metadata, all repetitions, correctness
verification, and a short methodology summary.
